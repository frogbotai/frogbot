import { describe, it } from 'vitest';

describe('frogbot listen', () => {
  it.todo('starts the Hono app on the given port via @hono/node-server');
  it.todo('returns a shutdown function');
  it.todo('shutdown closes the underlying server and resolves');
  it.todo('shutdown rejects with the underlying error when server.close fails');
});
