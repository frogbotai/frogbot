---
title: JSON Field
sidebarTitle: JSON
description: Store structured JSON with optional schema validation and editor assistance.
keywords: json, json schema, validation, fields, collections, configuration
---

The JSON field stores JSON values directly. In the admin panel, editors work in a code editor with syntax highlighting. Use a [Code field](/fields/code) instead when the database value must always be a string.

```ts
import type { CollectionConfig } from 'frogbot'

export const Settings: CollectionConfig = {
  slug: 'settings',
  fields: [
    {
      name: 'theme',
      type: 'json',
      required: true,
    },
  ],
}
```

## Config options

| Option | Description |
| --- | --- |
| **`name`** * | Property name used in stored records and APIs. |
| **`jsonSchema`** | JSON Schema used for editor suggestions and validation. |
| **`label`** | Field label shown in the admin panel. |
| **`unique`** | Creates a database-level unique index. |
| **`index`** | Creates a database index. |
| **`validate`** | Custom [validation function](/fields/overview#validation). |
| **`saveToJWT`** | Includes a top-level auth field in the user token. |
| **`hooks`** | [Field hooks](/hooks/fields). |
| **`access`** | [Field access control](/access-control/fields). |
| **`hidden`** | Hides the field from APIs and the admin panel while retaining stored data. |
| **`defaultValue`** | [Initial field value](/fields/overview#default-values). |
| **`localized`** | Stores a value for each configured locale. |
| **`required`** | Requires a value. |
| **`admin`** | Admin-panel appearance and behavior. |
| **`custom`** | Custom metadata available to extensions. |
| **`typescriptSchema`** | JSON Schema override used during type generation. |
| **`virtual`** | Set to `true` to omit the field from database storage. |

\*_An asterisk denotes a required option._

## Admin options

`admin.editorOptions` accepts Monaco editor options:

```ts
const preferencesField = {
  name: 'preferences',
  type: 'json',
  admin: {
    editorOptions: {
      minimap: { enabled: false },
      tabSize: 2,
    },
  },
} as const
```

## JSON Schema validation

Provide a local schema to add completion hints and reject invalid values when a record is saved:

```ts
import type { CollectionConfig } from 'frogbot'

export const Customers: CollectionConfig = {
  slug: 'customers',
  fields: [
    {
      name: 'preferences',
      type: 'json',
      jsonSchema: {
        uri: 'app://schemas/preferences.json',
        fileMatch: ['app://schemas/preferences.json'],
        schema: {
          type: 'object',
          additionalProperties: false,
          properties: {
            colorScheme: {
              enum: ['light', 'dark', 'system'],
            },
          },
        },
      },
    },
  ],
}
```

A schema can also be loaded from a public URL by providing `uri` and `fileMatch` without an inline `schema`. Prefer an inline or imported schema when availability must not depend on a remote server.

## Custom components

Override the field or its label through `admin.components`. Custom components can wrap FrogBot's default JSON editor.

### Field

#### Server component

```tsx
import { JSONField } from '@frogbotai/ui'
import type { JSONFieldServerComponent } from 'frogbot'

export const CustomJSONFieldServer: JSONFieldServerComponent = ({
  clientField,
  path,
  permissions,
  schemaPath,
}) => (
  <JSONField
    field={clientField}
    path={path}
    permissions={permissions}
    schemaPath={schemaPath}
  />
)
```

#### Client component

```tsx
'use client'

import { JSONField } from '@frogbotai/ui'
import type { JSONFieldClientComponent } from 'frogbot'

export const CustomJSONFieldClient: JSONFieldClientComponent = (props) => (
  <JSONField {...props} />
)
```

### Label

#### Server component

```tsx
import { FieldLabel } from '@frogbotai/ui'
import type { JSONFieldLabelServerComponent } from 'frogbot'

export const CustomJSONLabelServer: JSONFieldLabelServerComponent = ({
  clientField,
  path,
}) => (
  <FieldLabel
    label={clientField.label || clientField.name}
    path={path}
    required={clientField.required}
  />
)
```

#### Client component

```tsx
'use client'

import { FieldLabel } from '@frogbotai/ui'
import type { JSONFieldLabelClientComponent } from 'frogbot'

export const CustomJSONLabelClient: JSONFieldLabelClientComponent = ({ field, path }) => (
  <FieldLabel label={field.label || field.name} path={path} required={field.required} />
)
```
