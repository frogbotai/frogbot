---
title: "OpenCode"
description: "Use your FrogBot subscription with OpenCode"
---

OpenCode is an open-source AI coding agent. Connect your FrogBot subscription to access all supported models with one API key.

---

## Set up

1. Head over to the [FrogBot dashboard](https://app.frogbot.ai/signup), create an account, and generate an API key

2. Run the `/connect` command and search for **FrogBot**

   ```txt
   /connect
   ```

3. Enter your FrogBot API key

   ```txt
   ┌ API key
   │
   │
   └ enter
   ```

4. Run the `/models` command to select a model

   ```txt
   /models
   ```

---

## Switch models

Use `/models` anytime to switch between available models. All models listed in the [FrogBot docs](/) are available through the connection.

---

## Learn more

<Card
  title="OpenCode docs"
  icon="book"
  href="https://opencode.ai/docs/providers/#frogbot"
>
  View the FrogBot provider documentation on OpenCode
</Card>
